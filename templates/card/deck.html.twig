{% extends "base.html.twig" %}

{% block title %}Card Game!{% endblock %}

{% block body %}
    <h1>Cards in deck</h1>
    <div class="deck-container">
    {% for card in cards %}
        <div class="{{card}}"><h3>{{lowRes[loop.index - 1]}}</h3><h3>{{lowRes[loop.index - 1]}}</h3></div>
    {% endfor %}
    </div>
{% endblock %}
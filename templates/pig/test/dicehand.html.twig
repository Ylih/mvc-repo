{% block body %}
<h1>Roll many dice</h1>

<p>You rolled {{ dices }} dices. Here are the value of the dices.</p>

<ul>
{% for roll in diceRoll %}
    <li>{{ roll }}</li>
{% endfor %}
</ul>

<p>Try to roll 
    <a href="{{ path('test_dicehand', {num: 5}) }}">5</a>
    or 
    <a href="{{ path('test_dicehand', {num: 10}) }}">10</a>
    dices.
</p>

{% endblock %}